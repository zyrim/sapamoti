{% extends 'base.html.twig' %}

{% block body %}
    <h1>Accounts</h1>
    <a href="{{ url("finance_account_add") }}">Account hinzufügen</a>
    {% for account in accounts %}
        <h2><a href="{{ url('finance_account', {'financeAccountId': account.account.financeAccountId}) }}">{{ account.account.name }}</a></h2>
        <p>({{ account.movements }}) {{ account.amount|movement|raw }}</p>
    {% endfor %}
{% endblock %}