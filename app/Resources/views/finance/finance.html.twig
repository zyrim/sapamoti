{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col-lg-12">
            <h1><a href="{{ url('account', {'id': account.financeAccountId}) }}">{{ account.name }}</a></h1>
            <h2>{{ account.amount|movement|raw }}</h2>
        </div>
    </div>
    <div class="row">
        <div class="col-lg-2">
            <div class="list-group">
                <a class="list-group-item {{ routeMatch('finance_edit') }}" href="{{ url('finance_edit', {'id': account.financeAccountId}) }}">Konto bearbeiten</a>
                <a class="list-group-item {{ routeMatch('account_movements') }}" href="{{ url('account_movements', {'id': account.financeAccountId}) }}">Umsätze</a>
            </div>
        </div>
        <div class="col-lg-10">
            {{ include(template) }}
        </div>
    </div>
{% endblock %}